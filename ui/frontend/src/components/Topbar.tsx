import React from "react";

const Topbar = () => {
    return (
        <header className="bg-blue-600 text-white px-6 py-4 shadow-md">
            <h1 className="text-xl font-semibold">Monify Dashboard</h1>
        </header>
    );
};

export default Topbar;
